# data/carol/job_profiles/

Crie aqui um arquivo `.txt` por perfil de vaga-alvo da Carol (ex:
`data_scientist.txt`, `product_manager.txt`), no mesmo formato usado em
`data/ricardo/job_profiles/`. O nome do arquivo (sem `.txt`) vira o valor de
`--role`.
