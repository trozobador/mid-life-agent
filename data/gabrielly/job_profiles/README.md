# data/gabrielly/job_profiles/

Crie aqui um arquivo `.txt` por perfil de vaga-alvo da Gabrielly (ex:
`analista_financeiro.txt`, `product_manager.txt`), no mesmo formato usado em
`data/ricardo/job_profiles/`. O nome do arquivo (sem `.txt`) vira o valor de
`--role`.
