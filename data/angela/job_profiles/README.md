# data/angela/job_profiles/

Crie aqui um arquivo `.txt` por perfil de vaga-alvo da Angela (ex:
`secretaria.txt`, `recepcionista.txt`), no mesmo formato usado em
`data/ricardo/job_profiles/`. O nome do arquivo (sem `.txt`) vira o valor de
`--role`.
